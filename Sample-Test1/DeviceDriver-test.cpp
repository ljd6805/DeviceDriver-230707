#include "gtest/gtest.h"
#include "gmock/gmock.h"
#include "../Project10/DeviceDriver.h"
using namespace testing;

TEST(TestCaseName, TestName) {
  EXPECT_EQ(1, 1);
  EXPECT_TRUE(true);
  EXPECT_THAT(1, 1);
}